# ===== 必填 =====
# 镜像主域名（DNS A 记录指向本机；80/443 可达，Caddy 自动 HTTP-01 出证）
MIRROR_HOST=hf.northstar.cool

# ===== 仅启用 xet.Caddyfile（第三层）时必填 =====
# 三个扁平二级子域，各加一条 A 记录指向本机；不启用就保持注释。
# 走默认 HTTP-01 时 Caddy 为每个域名单独出证，无需泛域名证书。
XETCDN_HOST=hfcdn.northstar.cool
XETCAS_HOST=hf-cas.northstar.cool
XETDL_HOST=hf-xetdl.northstar.cool

# ===== 仅泛域名 DNS-01 证书（可选）=====
# 前提：northstar.cool 的 NS 托管在 Cloudflare，且已创建仅含
# Zone:DNS:Edit 权限（限定本 zone）的 API Token。
# 启用后在主 Caddyfile 全局块取消 acme_dns cloudflare 注释。
# CF_TOKEN=
